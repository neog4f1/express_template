{
  "name": "express-postgres",
  "version": "1.0.0",
  "main": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "ts-node-dev --transpile-only",
    "start": "NODE_ENV=production ts-node"
  },
  "dependencies": {
    "express": "*",
    "pg": "*",
    "cors": "*"
  },
  "devDependencies": {
    "ts-node-dev": "*"
  }
}
